---
tags: [changelog, track-b, milestone]
commit: pending
branch: track-b-intel
date: 2026-07-19
---

# 🎉 Milestone: Database Live — Full System Verified End-to-End

> [!info] What happened
> User created the **Neon Postgres** and added `DATABASE_URL`. Schema pushed (`prisma db push`), seed ran (4 personas + thesis), and every API route was exercised live against the real stack: HTTP → pipeline → OpenAI → persistence → traceability.

## Verified live, in order

1. **`GET /api/opportunities`** — served seeded deals (WarehouseCV, CampusCartel).
2. **`POST /api/opportunities/[id]/run`** (Amara) — full pipeline: screen `proceed`, composite **70.2 [66.1-74.6]**, gap **+63.5**, ambition `substantial`/`back_the_person`, axes 70/55/64, memo `request_info` with 7 gaps, signal→decision 88.4h. All persisted: score row + ambitionRead, 6 ScoreHistory rows, 6 InterviewQuestions, claims updated by validator.
3. **`POST /api/founders/[id]/signals`** with interview notes (demo beat 5!) — 8 claims extracted, 3 dimensions delta-updated with rationales (execution 70→77 [73-81]), composite recomputed 70.2→72.6, gap 63.5→65.9, history appended with `causeSignalId`.
4. **`POST /api/apply`** re-application with Amara's email — **`returningFounder: true`**, same founderId, new opportunity starts from her existing score. FAQ 6 persistence proven.
5. **ReasoningLog**: 28 rows, every step traced — the 5×3 self-consistency structure visible in the counts. Cache is now active (identical calls will be free).

## Still needed

- Track A Gate 4 importer can now run against the shared DB (36-profile corpus → Postgres).
- Vercel connection (user step) — env vars ready.
- Streaming memo endpoint · outbound-scan endpoint · Track C screens.

Prev: [[2026-07-19-persistence-and-api]]
